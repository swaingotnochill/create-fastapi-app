from fastapi import FastAPI
{% if has_metrics %}from prometheus_client import start_http_server{% endif %}

app = FastAPI(title="My first FastAPI app")

@app.get("/")
def read_root():
    return {"hello": "world"}

